import 'package:flutter/material.dart';

class SkillDetailsForm extends StatelessWidget {
  const SkillDetailsForm({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Skill Details Form"),
    );
  }
}

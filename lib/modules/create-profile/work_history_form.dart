import 'package:flutter/material.dart';

class WorkHistoryForm extends StatelessWidget {
  const WorkHistoryForm({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}

import 'package:flutter/material.dart';

class EducationDetailsForm extends StatelessWidget {
  const EducationDetailsForm({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}

import 'package:flutter/material.dart';

class SummaryDetailsForm extends StatelessWidget {
  const SummaryDetailsForm({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
